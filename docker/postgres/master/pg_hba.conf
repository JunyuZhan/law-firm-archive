# PostgreSQL 主库访问控制配置
# 允许从服务器进行复制

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 本地连接
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# 允许从服务器复制（请根据实际IP修改）
# 格式: host replication <用户名> <从服务器IP>/32 md5
host    replication     replicator      192.168.50.11/32        md5

# 允许应用连接（请根据实际IP修改）
host    all             law_admin       192.168.50.0/24         md5
