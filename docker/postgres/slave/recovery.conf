# PostgreSQL 从库恢复配置
# 此文件会被挂载到容器的数据目录

# 主库连接信息（请根据实际情况修改）
primary_conninfo = 'host=192.168.50.10 port=5432 user=replicator password=replicator_password'

# 启用热备模式
standby_mode = on

# 复制槽名称（可选，用于防止主库删除未复制的 WAL）
# primary_slot_name = 'replication_slot'
