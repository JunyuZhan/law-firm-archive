# 🚀 服务器部署完整指南

## 📋 部署前准备

### 1. 服务器要求

**最低配置：**
- CPU: 2核
- 内存: 4GB
- 磁盘: 20GB
- 操作系统: Ubuntu 20.04+ / CentOS 7+ / Debian 10+

**推荐配置：**
- CPU: 4核+
- 内存: 8GB+
- 磁盘: 50GB+ SSD

### 2. 需要开放的端口

| 端口 | 协议 | 说明 |
|------|------|------|
| 22 | TCP | SSH（远程管理） |
| 80 | TCP | HTTP |
| 443 | TCP | HTTPS |
| 9001 | TCP | MinIO 控制台（可选，建议内网访问） |

---

## 🔧 步骤一：服务器环境准备

### 1.1 连接到服务器

```bash
ssh root@你的服务器IP
```

### 1.2 安装 Docker 和 Docker Compose

**Ubuntu/Debian:**
```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 启动 Docker
systemctl start docker
systemctl enable docker

# 安装 Docker Compose
apt install docker-compose-plugin -y

# 验证安装
docker --version
docker compose version
```

**CentOS/RHEL:**
```bash
# 更新系统
yum update -y

# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 启动 Docker
systemctl start docker
systemctl enable docker

# 安装 Docker Compose
yum install docker-compose-plugin -y

# 验证安装
docker --version
docker compose version
```

### 1.3 配置防火墙

```bash
# Ubuntu/Debian (ufw)
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable

# CentOS/RHEL (firewalld)
firewall-cmd --permanent --add-port=22/tcp
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload
```

---

## 📦 步骤二：上传代码到服务器

### 方式一：使用 Git（推荐）

```bash
# 在服务器上克隆代码
cd /opt  # 或其他目录
git clone https://github.com/JunyuZhan/law-firm.git
cd law-firm
```

**⚠️ 重要提示：** 代码拉取后，SSL 证书文件**不会**自动下载（因为已在 `.gitignore` 中），需要单独上传证书！

### 方式二：使用 SCP 上传

```bash
# 在本地电脑执行
cd /Users/apple/Documents/Project/law-firm
scp -r . root@你的服务器IP:/opt/law-firm
```

### 方式三：使用 rsync（推荐，支持增量同步）

```bash
# 在本地电脑执行
cd /Users/apple/Documents/Project/law-firm
rsync -avz --exclude 'node_modules' --exclude '.git' \
  . root@你的服务器IP:/opt/law-firm
```

---

## 🔐 步骤三：配置 SSL 证书

**⚠️ 重要：** 证书文件**不会**通过 Git 同步，必须单独上传！

### 3.1 上传证书文件

**方式一：使用上传脚本（推荐，最简单）**

```bash
# 在本地项目目录执行
./scripts/upload-ssl-certs.sh <服务器IP> [用户名]

# 例如：
./scripts/upload-ssl-certs.sh 192.168.1.100 root
```

脚本会自动：
- ✅ 检查本地证书文件
- ✅ 上传到服务器
- ✅ 创建符号链接
- ✅ 设置正确的文件权限

**方式二：手动上传（使用 SCP）**

```bash
# 在服务器上创建证书目录
ssh root@你的服务器IP "mkdir -p /opt/law-firm/docker/ssl"

# 在本地执行，上传证书文件
scp docker/ssl/ca.crt root@你的服务器IP:/opt/law-firm/docker/ssl/
scp docker/ssl/ca.key root@你的服务器IP:/opt/law-firm/docker/ssl/
```

**方式三：在服务器上使用 Let's Encrypt（推荐生产环境）**

```bash
# SSH 到服务器
ssh root@你的服务器IP

# 安装 certbot
apt install certbot -y

# 获取证书（需要域名已解析到服务器）
certbot certonly --standalone -d 你的域名.com

# 复制证书到项目目录
mkdir -p /opt/law-firm/docker/ssl
cp /etc/letsencrypt/live/你的域名.com/fullchain.pem /opt/law-firm/docker/ssl/fullchain.pem
cp /etc/letsencrypt/live/你的域名.com/privkey.pem /opt/law-firm/docker/ssl/privkey.pem
```

### 3.2 创建符号链接（如果使用 ca.crt）

```bash
cd /opt/law-firm/docker/ssl
ln -sf ca.crt fullchain.pem
ln -sf ca.key privkey.pem
chmod 644 fullchain.pem
chmod 600 privkey.pem
```

---

## ⚙️ 步骤四：配置环境变量

```bash
cd /opt/law-firm/docker

# 复制环境变量模板
cp env.example .env

# 编辑环境变量（重要！）
vim .env  # 或使用 nano .env
```

**必须修改的配置：**

```bash
# 数据库密码（必须修改！）
DB_PASSWORD=你的强密码

# JWT 密钥（必须修改！至少64字符）
JWT_SECRET=你的JWT密钥（使用 openssl rand -base64 64 生成）

# MinIO 密钥（必须修改！）
MINIO_ACCESS_KEY=你的访问密钥
MINIO_SECRET_KEY=你的秘密密钥

# MinIO 浏览器访问端点（如果通过域名访问）
MINIO_BROWSER_ENDPOINT=http://你的域名:9000
```

**生成安全密钥：**

```bash
# 生成数据库密码
openssl rand -base64 24

# 生成 JWT 密钥
openssl rand -base64 64

# 生成 MinIO 密钥
openssl rand -base64 24
```

---

## 🚀 步骤五：部署应用

### 方式一：一键部署（推荐）

```bash
cd /opt/law-firm

# 给脚本执行权限
chmod +x scripts/deploy.sh

# 执行部署
./scripts/deploy.sh
```

脚本会自动：
- ✅ 检查环境
- ✅ 生成安全密钥（如果未配置）
- ✅ 构建前端和后端
- ✅ 启动所有服务
- ✅ 初始化数据库

### 方式二：手动部署

```bash
cd /opt/law-firm/docker

# 1. 构建并启动服务
docker compose -f docker-compose.prod.yml up -d --build

# 2. 查看日志
docker compose -f docker-compose.prod.yml logs -f

# 3. 检查服务状态
docker compose -f docker-compose.prod.yml ps
```

---

## 🔒 步骤六：配置 HTTPS（如果使用 SSL 证书）

### 6.1 修改 Dockerfile 使用 SSL 配置

编辑 `frontend/scripts/deploy/Dockerfile`，将第58行改为：

```dockerfile
# 复制 nginx 配置（使用 SSL 版本）
COPY --from=builder /app/scripts/deploy/nginx-ssl.conf /etc/nginx/nginx.conf
```

### 6.2 重新构建前端

```bash
cd /opt/law-firm/docker
docker compose -f docker-compose.prod.yml up -d --build frontend
```

### 6.3 验证 HTTPS

访问 `https://你的服务器IP` 或 `https://你的域名`

---

## ✅ 步骤七：验证部署

### 7.1 检查服务状态

```bash
cd /opt/law-firm/docker
docker compose -f docker-compose.prod.yml ps
```

所有服务应该显示 `Up` 状态。

### 7.2 检查日志

```bash
# 查看所有服务日志
docker compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker compose -f docker-compose.prod.yml logs -f backend
docker compose -f docker-compose.prod.yml logs -f frontend
```

### 7.3 测试访问

```bash
# 测试 HTTP
curl http://localhost

# 测试 HTTPS（如果配置了）
curl -k https://localhost

# 测试 API
curl http://localhost/api/actuator/health
```

### 7.4 浏览器访问

- 主应用: `http://你的服务器IP/` 或 `https://你的域名/`
- 文档站点: `http://你的服务器IP/docs/`
- MinIO 控制台: `http://你的服务器IP:9001/`

**默认账号密码：** `admin123`

---

## 🔄 步骤八：日常维护

### 更新代码

```bash
cd /opt/law-firm

# 拉取最新代码
git pull

# 重新部署
cd docker
docker compose -f docker-compose.prod.yml up -d --build
```

### 查看日志

```bash
cd /opt/law-firm/docker

# 查看所有日志
docker compose -f docker-compose.prod.yml logs -f

# 查看最近100行日志
docker compose -f docker-compose.prod.yml logs --tail=100
```

### 重启服务

```bash
cd /opt/law-firm/docker

# 重启所有服务
docker compose -f docker-compose.prod.yml restart

# 重启单个服务
docker compose -f docker-compose.prod.yml restart backend
```

### 停止服务

```bash
cd /opt/law-firm/docker

# 停止所有服务（保留数据）
docker compose -f docker-compose.prod.yml stop

# 停止并删除容器（数据保留在 volumes 中）
docker compose -f docker-compose.prod.yml down
```

### 备份数据

```bash
# 备份数据库
docker exec law-firm-postgres pg_dump -U law_admin law_firm > backup_$(date +%Y%m%d).sql

# 备份 MinIO 数据
docker exec law-firm-minio mc mirror /data /backup/minio
```

---

## 🐛 常见问题排查

### 问题1：服务无法启动

```bash
# 查看详细日志
docker compose -f docker-compose.prod.yml logs service-name

# 检查端口占用
netstat -tlnp | grep :80
netstat -tlnp | grep :443

# 检查磁盘空间
df -h
```

### 问题2：数据库连接失败

```bash
# 检查数据库容器状态
docker logs law-firm-postgres

# 测试数据库连接
docker exec law-firm-postgres pg_isready -U law_admin -d law_firm

# 检查环境变量
docker exec law-firm-backend env | grep DB_
```

### 问题3：前端无法访问后端

```bash
# 检查后端健康状态
curl http://localhost/api/actuator/health

# 检查 Nginx 配置
docker exec law-firm-frontend nginx -t

# 查看 Nginx 日志
docker logs law-firm-frontend
```

### 问题4：SSL 证书错误

```bash
# 检查证书文件是否存在
ls -la /opt/law-firm/docker/ssl/

# 检查证书是否挂载到容器
docker exec law-firm-frontend ls -la /etc/nginx/ssl/

# 测试证书有效性
openssl x509 -in /opt/law-firm/docker/ssl/fullchain.pem -text -noout
```

---

## 📞 获取帮助

如果遇到问题：

1. 查看日志：`docker compose -f docker-compose.prod.yml logs`
2. 检查文档：`docs/` 目录下的相关文档
3. 查看 GitHub Issues

---

## 🎉 部署完成！

恭喜！你的系统已经部署完成。现在可以：

1. 访问系统：`http://你的服务器IP/`
2. 登录系统：用户名 `admin`，密码 `admin123`
3. 开始使用！

**重要提醒：**
- ⚠️ 首次登录后请立即修改默认密码
- ⚠️ 生产环境请使用正式 SSL 证书
- ⚠️ 定期备份数据库和文件
