# Gemini SVG 插画生成提示词 - 简化版（背景透明）

## 🎯 推荐提示词（3-4个核心元素）

```
请生成一个法律行业管理系统的登录页面SVG插画代码。

**核心要求：**
1. **背景必须透明**：不要添加任何背景矩形（rect）或背景色，SVG背景应该是完全透明的
2. **元素数量**：3-4个核心元素即可，保持简洁专业
3. **风格**：等距投影（isometric）3D风格，扁平化设计，类似Undraw.co
4. **尺寸**：viewBox="0 0 800 800"

**随机包含的核心元素（3-4个）：**
1. **法律书籍**（2-3本堆叠，等距投影，深蓝色渐变 #1E40AF 到 #3B82F6）- 主要元素
2. **天平**（正义象征，等距投影，金色 #F59E0B，位于书籍上方）- 核心象征
3. **文件夹/卷宗**（1个，部分打开，紫色 #7C3AED）- 辅助元素
4. **发光平台/底座**（圆形或方形，径向渐变，蓝色光晕，作为主要元素的支撑）

**不要包含：**
- ❌ 背景矩形或背景色
- ❌ 数据图表（可选，如果空间允许）
- ❌ 云朵图标
- ❌ 人物形象
- ❌ 装饰性背景形状

**配色方案：**
- 主色：深蓝色 #1E40AF, #2563EB, #3B82F6
- 辅助色：紫色 #7C3AED, #6D28D9
- 强调色：金色 #F59E0B, #D97706
- 背景：**完全透明**（不添加任何背景）

**技术要求：**
- 完整的SVG代码，包含XML命名空间
- 在<defs>中定义渐变和滤镜（不要定义背景渐变）
- 使用<g>标签组织元素
- 使用线性渐变和径向渐变创建立体感
- 使用滤镜创建阴影和光晕效果
- 所有ID唯一且有意义

**代码结构：**
```xml
<svg enable-background="new 0 0 800 800" version="1.1" viewBox="0 0 800 800" 
     xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
  <defs>
    <!-- 渐变定义（不要背景渐变） -->
    <!-- 滤镜定义 -->
  </defs>
  <!-- 直接开始绘制元素，不要背景矩形 -->
  <g id="main-content">
    <!-- SVG内容 -->
  </g>
</svg>
```

**输出要求：**
- 只输出完整的SVG XML代码，不要任何解释文字
- 确保背景完全透明
- 元素简洁，3-4个核心元素即可
- 代码格式良好，有适当缩进
```

## 📊 当前元素分析

### 当前SVG包含的元素（6个）：
1. ✅ 法律书籍（3本堆叠）- **保留**
2. ✅ 天平（金色）- **保留**
3. ✅ 文件夹（1个）- **保留**
4. ⚠️ 数据图表（柱状图）- **可选，可移除**
5. ⚠️ 云朵图标 - **可移除**
6. ✅ 发光平台 - **保留**

### 💡 建议简化方案

**方案A：最简版（3个元素）**
- 法律书籍（2-3本）
- 天平（金色）
- 发光平台

**方案B：推荐版（4个元素）**
- 法律书籍（2-3本）
- 天平（金色）
- 文件夹（1个）
- 发光平台

**方案C：当前版（5-6个元素）**
- 法律书籍（3本）
- 天平
- 文件夹
- 数据图表
- 云朵
- 发光平台

## ✅ 已完成的修改

1. ✅ 已移除背景矩形（`<rect width="800" height="800" fill="url(#bg-gradient)" />`）
2. ✅ 已移除背景装饰形状（`bg-shapes`组）
3. ✅ SVG背景现在是透明的

## 🎨 元素数量建议

**推荐：3-4个核心元素**
- 法律书籍（主要元素）
- 天平（核心象征）
- 文件夹（辅助元素）
- 发光平台（支撑元素）

**当前：5-6个元素** - 稍微有点多，但可以接受

如果需要进一步简化，可以移除：
- 数据图表（非核心元素）
- 云朵图标（装饰性元素）

