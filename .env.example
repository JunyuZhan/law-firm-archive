# =====================================================
# 智慧律所管理系统 - 环境变量配置
# =====================================================
# 复制此文件为 .env 并修改配置值
# cp .env.example .env
# 
# ⚠️ 安全警告：
# - 不要将 .env 文件提交到版本控制
# - 所有密钥应使用强随机值
# - 定期轮换密钥
# =====================================================

# ============== 数据库配置 ==============
# 数据库密码（必须设置强密码）
# 生成方式: openssl rand -base64 24
DB_PASSWORD=your_secure_db_password_here

# ============== Redis配置 ==============
# Redis 密码（生产环境必须设置）
# 生成方式: openssl rand -base64 24
REDIS_PASSWORD=your_redis_password_here

# ============== JWT安全配置 ==============
# JWT 密钥（必须设置！至少64字符）
# 生成方式: openssl rand -base64 64
# ⚠️ 修改此密钥会使所有现有登录失效
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_here_at_least_64_characters

# ============== MinIO对象存储配置 ==============
MINIO_ACCESS_KEY=lawfirm_minio_admin
# MinIO 管理员密码（必须设置强密码）
# 生成方式: openssl rand -base64 24
MINIO_SECRET_KEY=your_secure_minio_password_here

# ============== OnlyOffice配置 ==============
# JWT 密钥（生产环境必须设置，与容器配置一致）
# 生成方式: openssl rand -hex 32
ONLYOFFICE_JWT_SECRET=your_onlyoffice_jwt_secret_change_in_production
# 启用 JWT 验证（生产环境必须为 true）
ONLYOFFICE_JWT_ENABLED=true
# 注意：OnlyOffice URL 无需配置，前端会自动检测当前访问的域名

# ============== OCR服务安全配置 ==============
# API Key（生产环境必须设置）
# 生成方式: openssl rand -hex 32
OCR_API_KEY=your_ocr_api_key_here
OCR_API_KEY_ENABLED=true
OCR_RATE_LIMIT_REQUESTS=30
OCR_RATE_LIMIT_WINDOW=60
OCR_MAX_FILE_SIZE=10485760

# ============== 安全配置 ==============
# API文档（生产环境必须关闭）
SWAGGER_ENABLED=false


# ============== 文档站点配置 ==============
# 文档站点登录用户名
DOCS_USERNAME=admin
# 文档站点登录密码（自动生成）
# 生成方式: openssl rand -base64 16
DOCS_PASSWORD=your_docs_password_here
