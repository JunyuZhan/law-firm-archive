-- ============================================
-- 合同与项目业务流程重构 - 数据库迁移脚本
-- 执行日期：2026-01-XX
-- ============================================

-- 1. 修改合同表：sign_date 改为可选（创建合同时可能还没有签约日期）
ALTER TABLE finance_contract 
    ALTER COLUMN sign_date DROP NOT NULL;

-- 2. 确保 matter_id 可以为空（合同可以先于项目存在）
-- 如果之前有 NOT NULL 约束，需要删除
-- 注意：如果表已经存在且 matter_id 已经是可选的，这个语句不会报错

-- 3. 添加注释说明
COMMENT ON COLUMN finance_contract.sign_date IS '签约日期（可选，创建合同时可能尚未签约）';
COMMENT ON COLUMN finance_contract.matter_id IS '关联案件ID（可选，合同可以先于项目存在）';

-- ============================================
-- 说明：
-- 1. sign_date 改为可选，因为合同创建时可能还没有签约
-- 2. matter_id 已经是可选的，支持合同先于项目存在
-- 3. 合同审批通过后，可以通过合同创建项目
-- ============================================

