# 核心业务用例台账

用于记录系统核心业务链路的标准测试用例及其执行状态，适合回归测试时逐条勾对。

## 用例状态说明

- `未测`：尚未执行
- `通过`：按预期通过
- `失败`：执行失败
- `阻塞`：受前置问题影响无法继续

## 核心用例

| 用例编号 | 业务域 | 用例名称 | 前置条件 | 核心步骤 | 预期结果 | 最近执行版本 | 当前状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-LOGIN-01 | 认证 | 系统管理员登录 | 系统已部署 | `admin/admin123` 登录并调用 `/api/auth/me` | 登录成功，用户身份正确 | v0.1.1 | 通过 | 已验证 |
| TC-LOGIN-02 | 认证 | 档案员登录 | 系统已部署 | `archivist1/admin123` 登录并调用 `/api/auth/me` | 登录成功，用户身份正确 | v0.1.1 | 通过 | 已验证 |
| TC-LOGIN-03 | 认证 | 普通用户登录 | 系统已部署 | `lawyer1/admin123` 登录并调用 `/api/auth/me` | 登录成功，用户身份正确 | v0.1.1 | 通过 | 已验证 |
| TC-OPEN-01 | 入库 | 开放健康检查 | 系统已部署 | 调用 `/api/open/health` | 返回成功 | v0.1.1 | 通过 | 已验证 |
| TC-OPEN-02 | 入库 | 开放接口接收档案 | 已配置有效 API Key | 调用 `/api/open/archive/receive` | 返回 `archiveId`、`archiveNo` | v0.1.1 | 通过 | v0.1.0 首测失败，已修复 |
| TC-STORE-01 | 保存 | 上传电子文件后档案详情同步显示 | 已存在测试档案 | 上传电子文件并查询档案详情 | `files` 不为空，`fileCount>0`，`hasElectronic=true` | v0.1.1 | 通过 | v0.1.0 首测失败，已修复 |
| TC-STORE-02 | 保存 | 档案状态更新为已归档 | 已创建测试档案 | 更新状态为 `STORED` | 状态更新成功 | v0.1.1 | 通过 | 已验证 |
| TC-BORROW-01 | 借阅 | 借阅可用性检查 | 档案已 `STORED` 且存在电子文件 | 调用 `/api/borrows/check/{archiveId}` | 返回 `available=true` | v0.1.1 | 通过 | 已验证 |
| TC-BORROW-02 | 借阅 | 普通用户提交在线借阅申请 | 档案可借阅 | 调用 `/api/borrows/apply` | 返回借阅申请ID | v0.1.1 | 通过 | 已验证 |
| TC-BORROW-03 | 借阅 | 档案员审批并借出 | 存在待审批借阅 | 审批通过并借出 | 状态推进成功 | v0.1.1 | 通过 | 已验证 |
| TC-BORROW-04 | 借阅 | 生成电子借阅链接 | 借阅已审批或借出 | 调用 `/api/borrow-links/generate` | 返回 `accessToken` | v0.1.1 | 通过 | 已验证 |
| TC-BORROW-05 | 借阅 | 公开访问电子借阅内容 | 已生成借阅链接 | 调用 `/api/open/borrow/access/{token}` | 返回 `valid=true` 及文件列表 | v0.1.1 | 通过 | 已验证 |
| TC-BORROW-06 | 借阅 | 获取预览链接 | 借阅链接有效且有文件 | 调用 `/api/open/borrow/access/{token}/preview/{fileId}` | 返回预览 URL | v0.1.1 | 通过 | 已验证 |
| TC-BORROW-07 | 借阅 | 下载型电子借阅 | 档案允许下载 | 申请 `DOWNLOAD` 并验证下载链路 | 返回下载地址或下载记录成功 | v0.1.3 | 通过 | 已验证 |
| TC-BORROW-08 | 借阅 | 续借 | 已存在借出申请 | 调用续借接口 | 新归还日期生效 | v0.1.3 | 通过 | 已验证 |
| TC-BORROW-09 | 借阅 | 归还 | 已借出 | 调用归还接口 | 归还成功 | v0.1.3 | 通过 | 已验证 |
| TC-INGEST-03 | 入库 | 批量入库 / 批次追溯 | 已准备批量数据 | 批量提交并检查批次 | 批次可追踪 | - | 未测 | 建议下一轮补测 |

## 当前推荐回归最小集

每次发版至少执行以下用例：

- `TC-LOGIN-01`
- `TC-OPEN-02`
- `TC-STORE-01`
- `TC-BORROW-01`
- `TC-BORROW-02`
- `TC-BORROW-03`
- `TC-BORROW-05`
